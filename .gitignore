# Python
__pycache__/
*.pyc
*.pyo

# CMake build artifacts (if the extension is ever built as a superbuild)
build/
CMakeCache.txt
CMakeFiles/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local scratch output some users may generate while testing
*.log

# Model checkpoints -- too large for git (235 MB each).
# Download from the GitHub Release and place under models/
*.pth
